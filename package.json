{
  "name": "cypress-todo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "concurrently": "^4.0.1",
    "json-server": "^0.14.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "serve": "json-server db.json",
    "dev": "concurrently \"yarn run serve\" \"yarn run start\"",
    "build": "react-scripts build",
    "cypress": "cypress open",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "cypress": "2.1.0"
  }
}
